---
title: React源码-useState
description: React源码-useState
tags:
  - React
  - 源码
keywords:
  - Reactid: react-useState
create: 1715411535354
lastUpdate: 1716359449128
---

# React源码-useState

 阅读react里中useState功能实现

## 准备工作

- 下载react源码，并执行构建工作

- 起一个简单的react项目，并link源码打出来的包

### 开始
1. 给项目打个断点
 <img src="/md-images/react-breakpoint.png" alt="先给项目打个断点"  />
2. 运行项目，可以看到如下的函数调用栈
 <img src="/md-images/react-func-stack.png" alt="先给项目打个断点"  />

- workLoop
  判断render是并发还是同步的
  var exitStatus = shouldTimeSlice ? renderRootConcurrent(root, lanes) : renderRootSync(root, lanes);

- renderRootSync
  - prepareFreshStack
    var rootWorkInProgress = createWorkInProgress(root.current, null);
    workInProgress = rootWorkInProgress;
  - createWorkInProgress
- workLoopSync
- performUnitOfWork

- beginWork$1 
  第一个参数为unitOfWork的alternate
- beginWork
  判断了props和context是否有变化,否则标记didReceiveUpdate = true;
  我们是第一次执行 所以执行了didReceiveUpdate = false;
  首次进来workInProgress.tag == IndeterminateComponent(2) (不确定组件类型)
  调用mountIndeterminateComponent
- mountIndeterminateComponent
  根据 prototype上是否有render函数判定是否为Class组件
- renderWithHook
- App
- useState
  HooksDispatcherOnMountInDEV中的useState


