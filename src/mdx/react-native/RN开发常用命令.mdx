---
title: RN开发常用命令
topLevel: true
tags:
  - shell
md5: c4d652c713fb6f2a79c879c9b8c434c8
updateTime: '2025-12-02 16:19:13'
createTime: '2025-11-10 15:19:25'
---

# RN开发常用命令

### 项目初始化
- 初始化RN项目
    ```cmd
        npx @react-native-community/cli@latest init demo --version 0.81.5
    ```
### adb相关

- adb查看连接的设备
    ```shell
        adb devices
    ```

- abd打开RN开发者菜单
    ```shell
        adb shell input keyevent 82
    ``` 
- Android 设备和开发者的机器之间创建端口转发。
    ```shell
        adb -s 设备id reverse tcp:端口 tcp:端口
    ``` 
- 查看芯片架构
    ```shell
        adb shell getprop ro.product.cpu.abi
    ```

### pod相关

### 网络相关

- ios查看ip地址
    ```shell
        ifconfig
    ```
### Android相关

- Android依赖树

    ```shell
        ./gradlew  app:dependencies
    ```
